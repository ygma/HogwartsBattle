import { ActiveHeroLoseHeart } from './active-hero-lose-heart';

describe('ActiveHeroLoseHeart', () => {
  it('should create an instance', () => {
    expect(new ActiveHeroLoseHeart()).toBeTruthy();
  });
});
