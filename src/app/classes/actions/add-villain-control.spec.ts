import { AddVillainControl } from './add-villain-control';

describe('AddVillainControl', () => {
  it('should create an instance', () => {
    expect(new AddVillainControl()).toBeTruthy();
  });
});
