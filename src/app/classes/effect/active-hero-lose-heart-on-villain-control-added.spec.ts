import { ActiveHeroLoseHeartOnVillainControlAdded } from './active-hero-lose-heart-on-villain-control-added';

describe('ActiveHeroLoseHeartOnVillainControlAdded', () => {
  it('should create an instance', () => {
    expect(new ActiveHeroLoseHeartOnVillainControlAdded()).toBeTruthy();
  });
});
